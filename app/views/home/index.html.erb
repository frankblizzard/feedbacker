<% title "Image Feedbacker - Home" %> 

<h2>Your projects, <%= current_user.username %> </h2>
<br/>

<table class="projects">
	<tr>
		<th>Project Name</th>
		<th>Started at</th>
		<th>Ends at</th>
		<th>Details</th>
	</tr>
<% @projects.each do |p| %>
	<tr>
			<td><%= "#{p.name}" %></td>
			<td><%= "#{p.started_at.to_formatted_s(:short)}" %></td>
			<td><%= "#{p.ends_at.to_formatted_s(:short)}" %></td>			
			<td><%= link_to "view", p %></td>
	<tr>
<% end -%>      
</table>

<br/>
<h2>Newest Images</h2>